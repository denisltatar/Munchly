# Munchly 🍴
Native iOS app (Swift + SwiftUI)

## Requirements
- Xcode (latest stable)
- iOS <set your minimum> SDK

## Run
1. Open `Munchly.xcodeproj` (or `.xcworkspace` if you add packages later)
2. Select **Munchly-Dev** scheme
3. Run on an iOS Simulator

## Testing
`⌘ + U` in Xcode, or use `xcodebuild` from CI.

## Folder Structure (starter)
- `Munchly/` — App code (SwiftUI)
- `MunchlyTests/` — Unit tests
- `MunchlyUITests/` — UI tests (optional)

## Roadmap
- [ ] Onboarding flow
- [ ] Basic meal logging
- [ ] Offline-friendly store
- [ ] Sync & auth
